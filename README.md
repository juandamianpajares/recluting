# BITNET Trainee - Starter Kit

Starter kit for the BITNET Trainee — Laravel 15D bootcamp.
Includes:
- Landing (Tailwind) + Panel views
- Trainee model, controller and migration
- Docker (docker-compose + Dockerfile + nginx)
- Provision script
- Certificate template (dompdf)
- Basic routes

Quick start:
1. Copy `.env.example` to `.env` and adjust env values.
2. Run `./provision.sh` (requires Docker).
3. Access app at http://localhost:8080 and Mailhog at http://localhost:8025
